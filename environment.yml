name: EA_env
channels:
  - conda-forge
  - defaults
dependencies:
  - matplotlib  
  - numpy   
  - pip
  - python=3.9
  - wheel 
  - scipy
  - tqdm
  - scikit-learn
  - sympy >= 1.9
  - joblib >= 1.1.0
  - notebook
  - swig
  - gymnasium[box2d]
  - tensorboard
  - setuptools==59.5.0